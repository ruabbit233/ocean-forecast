# 海冰预测演示系统现场演示稿

各位专家、老师上午好。接下来由我负责给各位演示系统，本次演示将按照顶部导航从左到右的顺序，依次展示系统的四个核心模块，分别是：**实时预报**、**航线规划**、**模型解释器**和**动力学分析**。演示重点放在系统功能展示、页面操作流程、参数含义以及结果呈现方式上。

---

## 一、实时预报

“首先演示的是实时预报模块。这里可以直观看到系统对北极海冰未来变化的可视化结果。页面支持两种时间尺度：一类是未来14天的逐日预报，适合展示短期变化过程；另一类是未来12个月的逐月预报，适合展示更长时间尺度下的整体趋势。通过播放功能，可以连续查看海冰范围和空间分布的动态演变。后台方面，逐日结果每天凌晨4点自动更新，逐月结果在每月17号凌晨4点自动更新。”

### 模块功能描述
实时预报模块用于展示系统生成的北极海冰预测结果，支持两类时间尺度：
- **逐日实时预报**：展示未来14天的逐日预测结果，用于观察短期海冰变化过程。
- **逐月实时预报**：展示未来12个月的逐月预测结果，用于查看更长时间尺度下的变化趋势。

该模块的核心作用，是把预测结果以球面可视化方式直接呈现出来，便于快速浏览海冰空间分布和时序演变。

### 任务更新说明
在介绍这个模块时，可以补充说明系统的数据更新机制：
- **逐月实时预报任务**：每月 **17号 4:00** 定时执行。
- **逐日实时预报任务**：每天 **4:00** 定时执行。



---

## 二、航线规划

“第二个模块是航线规划。这里可以选择出发时间、起点港口和终点港口，提交后系统会生成对应的航线规划任务。任务完成后，右侧会展示规划路径，同时给出航程距离、预计航时等结果信息。整个过程可以看到从参数设置、任务生成到结果加载的完整流程。”

### 模块功能描述
航线规划模块用于基于系统已有环境结果，生成指定起点、终点和出发时间条件下的航线规划结果。页面支持任务提交、状态跟踪和结果加载，形成完整的业务操作闭环。

该模块主要展示系统如何把环境信息进一步转化为可直接查看的航线结果。

### 页面参数及含义
该模块接收的主要参数包括：
- **出发日期**：表示航线规划所依据的出航时间。
- **起点港口**：表示船舶出发港。
- **终点港口**：表示目标到达港。

系统根据这些输入条件生成对应的航线规划任务，并在任务完成后返回规划结果。



---

## 三、模型解释器

“第三个模块是模型解释器。这里可以按照时间范围、预报间隔、分析目标、空间位置和变量来组织一次解释分析任务。比如我们可以指定某一段时间、某个预测提前期，并选择一个具体变量，再对一个区域进行框选，系统就会生成对应的解释结果。这样在现场可以比较直观地展示不同变量、不同区域下的解释分析效果。”

### 模块功能描述
模型解释器模块用于对模型输出结果进行解释性分析，帮助用户查看在特定时间范围、特定空间区域和特定变量条件下的解释结果。

这个模块的重点不是再次输出预测结果，而是展示模型解释性分析流程，即在给定输入条件后，系统如何生成对应的解释结果图。

### 页面参数及含义

该模块接收的主要参数包括：

- **时间范围**：用于限定分析所使用的数据时间窗口。
- **预报间隔天数**：表示分析时对应的预测提前期。
- **分析目标**：
  - **均值**：对应整体平均影响水平的展示。
  - **分布**：对应空间分布特征的展示。
- **选定位置**：通过鼠标框选分析区域，用于指定解释分析的空间范围。
- **选定变量**：用于指定参与解释分析的变量类型，当前页面可选变量包括：
  - 海冰密集度
  - 海冰速度 U 分量
  - 海冰速度 V 分量
  - 2米气温
  - 10米U风
  - 10米V风



---

## 四、动力学分析

“最后演示的是动力学分析模块。这个页面支持先选择时间范围和预报提前期，再框定具体分析区域，并设置分析目标。页面会自动生成对应的目标月份，提交之后系统会创建分析任务，并在右侧返回分析结果。整个过程展示了动力学分析模块的标准使用流程。”

### 模块功能描述
动力学分析模块用于围绕特定时间窗口、空间区域和分析目标，提交动力学分析任务并查看结果。该模块突出的是从参数设置到任务生成、再到结果查看的完整分析流程。

### 页面参数及含义
该模块接收的主要参数包括：
- **数据范围**：用于指定参与分析的月份区间。
- **预报提前期**：表示目标分析对应的提前期长度。
- **目标月份**：系统根据结束时间和预报提前期自动推导生成，用于提示本次分析所对应的目标月份。
- **分析范围**：通过框选方式指定分析区域。
- **分析目标**：
  - **海冰面积**
  - **海冰变化量**



---

## 五、后台增量学习任务说明

“除了前台可见的几个模块，系统后台还部署了增量学习任务。这个任务目前是每周一凌晨4点定时执行。基本流程是：系统自动获取最新数据集，对新数据进行预处理，然后在已有模型基础上做增量更新，完成之后再把更新后的模型用于后续预测和分析。这样可以保证系统能力随着新数据持续迭代，而不是长期停留在同一个模型版本上。”

### 模块功能描述
增量学习不在当前前台导航中直接展示，它属于系统后台定时运行的模型更新任务。现场介绍时，可以把它作为系统持续更新能力的一部分进行补充说明。

### 任务调度说明
- **增量学习任务**：每周一 **4:00** 定时执行。

### 基本流程说明
介绍时可以按照以下流程来讲：
1. 后台定时任务启动。
2. 系统下载最新数据集。
3. 对新增数据进行整理和预处理。
4. 在原有模型基础上进行增量更新，而不是完全重新训练。
5. 更新完成后，生成新的模型结果并进入后续业务链路。
6. 新模型继续服务于前台的实时预报及相关分析模块。

---

## 结束语

以上就是本次演示系统的主要功能展示。谢谢各位老师

